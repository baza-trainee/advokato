import PropTypes from 'prop-types';
import { LiStyled } from './InformationItem.styled';

export const InformationItem = ({}) => {
  return <LiStyled>aasdasdasd</LiStyled>;
};

InformationItem.propTypes = {
  data: PropTypes.array.isRequired,
};
